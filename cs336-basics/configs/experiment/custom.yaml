# @package _global_

paths:
  # Set to your data, tokenized with GPT-2 tokenizer!
  train_bin: /public/cs336/assignment4-data/data/minhash_dedup_en.np
  
  # The validation set which we have already tokenized with GPT-2 tokenizer, don't change this!
  valid_bin: /public/cs336/assignment4-data/data/minhash_dedup_en.np
  
  model_output: output/minhash_dedup_en

training:
  wandb_entity: pliang  # Set to your wandb entity, e.g. pliang
  wandb_project: cs336-data